version: '3'

services:
  # app:
  #   container_name: app
  #   build: ./app
  #   ports:
  #     - "8081:3000"
  #     - "3000:3000"
  #   links:
  #     - api
  #   volumes:
  #     - ./app:/usr/src/app/app
  nuxt:
    container_name: nuxt
    build: ./nuxt
    ports:
      - "8083:8083"
      - "3000:3000"
    links:
      - api
    volumes:
      - ./nuxt:/usr/src/app/nuxt
  api:
    container_name: api
    restart: always
    build: ./api
    ports:
      - '80:3000'
    links:
      - mongo
      - crawler
    volumes:
      - ./api:/usr/src/app/api
  mongo:
    container_name: mongo
    image: mongo
    ports:
      - '27017:27017'
    volumes:
      - ./mongo:/data/db
  crawler:
    restart: always
    build: ./crawler
    ports: 
      - '8082:8082'
    volumes:
      - ./crawler:/usr/src/app/crawler